import IconButton from './IconButton'
import S from '../stitches.config'

const IconPillButton = S.styled(IconButton, {
    borderRadius: '9999px',
})

IconPillButton.displayName = 'IconPillButton'

export default IconPillButton
